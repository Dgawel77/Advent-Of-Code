use advent::lib;
use std::io;

fn main() -> Result<(), io::Error>{{
    let lines = lib::read_lines("./in/day{DAY}input.txt").unwrap();
    for line in lines{{
        let ip = line?;
        
    }}
    Ok(())
}}